description "<%= @resource.service %>"

start on runlevel [2345]
stop on runlevel [016]

console log

exec <%= node.prometheus_client.install_dir%>/prometheus-node-exporter <%= @options.map { |k,v| "-#{k}='#{v}'" }.join(' ') %>

respawn
respawn limit 3 180