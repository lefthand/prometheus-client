---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-12.04

suites:
  - name: node_exporter
    run_list:
      - recipe[prometheus-client::node_exporter]
    attributes:
